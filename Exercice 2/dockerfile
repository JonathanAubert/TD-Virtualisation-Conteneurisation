# Utilise une image Node officielle
FROM node:18

# Crée le dossier de travail
WORKDIR /app

# Copie package.json et package-lock.json (si présent)
COPY package*.json ./

# Installe les dépendances
RUN npm install

# Copie le reste des fichiers (dont src/index.js)
COPY . .

# Expose le port 3030
EXPOSE 3030

# Démarre le serveur
CMD ["npm", "start"]
